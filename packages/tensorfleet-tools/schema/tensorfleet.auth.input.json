{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "command": {
            "type": "string",
            "enum": ["status", "login", "logout"],
            "description": "Auth command to execute. Defaults to 'login' for backward compatibility"
        },
        "backendUrl": {
            "type": "string",
            "description": "Optional custom backend URL for OAuth authentication. Defaults to https://app.tensorfleet.net/"
        },
        "cacheAuth": {
            "type": "boolean",
            "description": "Whether to load and store authentication using the auth cache. Defaults to true."
        },
        "forceLogin": {
            "type": "boolean",
            "description": "Ignore existing in-memory and cached authentication and start a new login flow."
        }
    },
    "required": []
}
