{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "node_id": { 
            "type": "string",
            "description": "Global ROS path of the ROS node to read from. Use `--list` to get a list of available nodes (and pass empty array for `parameters`)"
        },
        "parameters": {
            "type": "array",
            "items": { "type": "string" },
            "description": "A list of parameters to read from. Use `[\"--list\"]` to get a list of available parameters"
        },
        "return_type": {
            "type": "string",
            "default": "JSON",
            "description": "The return type for the response, defaults to JSON"
        },
        "tensorfleet-project-path": {
            "type": "string",
            "description": "Tensorfleet project directory path. The directory should contain both .tensorfleet and .env files at its root"
        }
    },
    "top_filter": {
        "type": "object",
        "description": "Top-level filter to apply to the query",
        "properties": {
            "field": {
                "type": "string",
                "description": "Field to filter on"
            },
            "operator": {
                "type": "string",
                "enum": ["eq", "ne", "gt", "gte", "lt", "lte", "in", "nin", "like", "regex"],
                "description": "Operator to use for filtering"
            },
            "value": {
                "type": "string",
                "description": "Value to filter by"
            }
        },
        "required": ["field", "operator", "value"],
        "additionalProperties": false
    },
    "required": ["node_id", "parameters", "tensorfleet-project-path"]
}
